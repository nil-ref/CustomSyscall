### Application to install the CustomSyscall Driver and execute our custom syscall

```
Usage: exe.exe <OPTION>
Options:
    help       Print this message.
    install    Load Driver
    uninstall  Unload Driver
    syscall    Issue the custom syscall
```
